using System;

namespace GenticAlgorithm
{
    // A gene contains the genetic information that describes an organism
    // A gene is it's own class so that it is interchangable and can contain
    // various representations
    class Genes
    {
        private int[] genes {get; set;} = new int[10];

        // Used during reproduction passing a portion of the genetic
        // information from the genes
        public int[] getChromosones(){
         return null;
        }

    }
}